# Backend API URL (without /api suffix - axios appends it automatically)
# NOTE: Vite only reads .env files at startup. You MUST restart the dev server
# after changing this value for it to take effect.
VITE_API_URL=http://localhost:5000

